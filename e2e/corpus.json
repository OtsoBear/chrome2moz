{
  "extensions": [
    {
      "id": "latextocalc",
      "name": "LatexToCalc",
      "source": "local:../LatexToCalc",
      "allowed_diffs": ["tabs.create:resolve"],
      "_notes": {
        "_history": "tabs.create:resolve used to be allowlisted here for the native Chrome-vs-Firefox tabs.Tab shape difference (Chrome-only: selected, frozen; Firefox-only: attention, hidden, isArticle, isInReaderMode, sharingState, successorTabId, cookieStoreId). Fixed properly in diff.ts's normalizeTrace instead: any Tab-shaped object (id+windowId+index+boolean active) is now projected down to {url, title, status, index, active} before comparison, so that entire class of divergence is gone.",
        "tabs.create:resolve": "Much smaller residual after the shape-projection fix: verified via re-run that the ONLY remaining difference is the `status` field (chrome: \"loading\", firefox: \"complete\") -- both sides otherwise now normalize identically (index, active match; url/title absent on both, matching this extension's permission scope). This is a genuine browser-native timing race in when each engine resolves the tabs.create() promise relative to the new tab's initial paint, not extension or converter behavior, and not fixable by adjusting probe delays (the capture happens synchronously in the shim's promise-then hook, with no artificial wait in between)."
      },
      "quarantined": false
    },
    {
      "id": "gojbdfnpnhogfdgjbigejoaolejmgdhk",
      "name": "OneNote Web Clipper",
      "source": "cws",
      "version": "3.11.2",
      "allowed_diffs": [
        "offscreen.*",
        "runtime.getContexts*",
        "runtime.error#for runtime.getContexts",
        "runtime.sendMessage#offscreen",
        "runtime.getManifest",
        "net.fetch#onenote.com/strings",
        "tabs.query",
        "tabs.create",
        "tabs.create:resolve",
        "tabs.onCreated:fired",
        "tabs.onUpdated:fired",
        "contextMenus.*",
        "management.uninstallSelf*"
      ],
      "_notes": {
        "_offscreen_cascade_root_cause": "OneNote's background script (minified from build/scripts/communicator/offscreenCommunicator.js) calls chrome.runtime.getContexts({contextTypes:[chrome.runtime.ContextType.OFFSCREEN_DOCUMENT], ...}) unconditionally, with no chrome.offscreen feature-detection guard. On Firefox, chrome.runtime.ContextType.OFFSCREEN_DOCUMENT is undefined (Firefox has no offscreen-document context type), so the call becomes getContexts({contextTypes:[undefined]}), which Firefox's WebExtensions API rejects synchronously ('Expected string instead of undefined'). The extension gates its entire post-install setup behind that same promise (clipperIdProcessed) via bare .then() with no .catch() anywhere (registerContextMenuItems(), onInstalled(), and invokeClipperInTab()'s actual clip-trigger path all chain off it), so on Firefox none of it ever runs: no offscreen document, no context menus, no 'getting started' tour tab, no translated-strings fetch, no clipperId relay -- and clipping itself silently never fires. This is a bug in OneNote's own source (unconditional use of a Chrome-only API), not a chrome2moz mis-conversion; filed as a converter-enhancement candidate at https://github.com/OtsoBear/chrome2moz/issues/2. All patterns below except management.uninstallSelf* are consequences of this single root cause.",
        "offscreen.*": "See _offscreen_cascade_root_cause. Firefox never reaches offscreen.createDocument at all (aborts earlier at getContexts), so this is chrome-only in the trace.",
        "runtime.getContexts*": "See _offscreen_cascade_root_cause. Covers runtime.getContexts / :resolve (chrome, succeeds) and :throw (firefox, the synchronous TypeError).",
        "runtime.error#for runtime.getContexts": "See _offscreen_cascade_root_cause -- the same getContexts TypeError also surfaces via the shim's global error/unhandledrejection listeners. Pinned to the observed message substring ('...for runtime.getContexts.') via the api#substring allowlist form, so an unrelated runtime.error for this extension is NOT masked.",
        "runtime.sendMessage#offscreen": "See _offscreen_cascade_root_cause -- all chrome-only calls carry target:\"offscreen\" payloads (clipperId read/set/remove relay to the offscreen document), never reached on Firefox. Pinned to the substring \"offscreen\" via api#substring so OneNote's other runtime.sendMessage call sites (contentCaptureInject.js, logManager.js, regionOverlay.js -- the actual clip flow, not currently probed) are NOT masked if probes are later extended to exercise them.",
        "runtime.getManifest": "See _offscreen_cascade_root_cause -- called from within the gated onInstalled() setup (chrome-only in the trace). Left name-only (not pinned): OneNote calls getManifest with no arguments, so there's no argument substring to pin to, and the API has no other call sites in this extension.",
        "net.fetch#onenote.com/strings": "See _offscreen_cascade_root_cause -- pinned to the exact observed URL (GET https://www.onenote.com/strings?... -- localized UI strings for the getting-started tour/context menu), fetched only from within the gated onInstalled() path. A net.fetch divergence to any other URL is NOT masked.",
        "tabs.query": "See _offscreen_cascade_root_cause -- used to find the active tab for the getting-started tour, inside the gated onInstalled() path. Call args only (a query filter object, not a Tab result), so the Tab-shape normalizer fix doesn't apply here -- Firefox simply never reaches this call at all.",
        "tabs.create": "See _offscreen_cascade_root_cause -- opens the getting-started tour tab (support.microsoft.com/.../getting-started-with-the-onenote-web-clipper-...), inside the gated onInstalled() path; never called on Firefox at all (not a shape difference -- the call itself doesn't happen), so the Tab-shape normalizer can't help here.",
        "tabs.create:resolve": "Verified via re-run (post shape-projection fix) this is now ONLY the offscreen-cascade tour tab -- chrome-only, since Firefox never calls tabs.create for it at all (zero events on that side, not a shape or timing mismatch). Distinct from latextocalc's tabs.create:resolve allowance, which is a real cross-browser timing race present on BOTH sides.",
        "tabs.onCreated:fired": "Two distinct sources, both verified via re-run: (1) the offscreen-cascade tour tab, chrome-only, same as tabs.create:resolve above. (2) OneNote registers its own tabs.onCreated/onUpdated listener (registerTabCreateOrUpdateListener in chromeExtension.js), so it also observes the harness's own probe-driven fixture-page tab (opened via Playwright/Selenium's own newPage(), not via tabs.create -- that's why this shows up here but tabs.create:resolve doesn't). That fixture tab exists on BOTH sides, but chrome's onCreated snapshot is {status:\"loading\", url:\"\", title:\"\"} while firefox's is {status:\"complete\", url:\"about:blank\", title:\"New Tab\"} for the same about:blank initial-paint moment -- a genuine browser-native convention difference in what state a just-created tab reports, not extension or converter behavior.",
        "tabs.onUpdated:fired": "Same two sources as tabs.onCreated:fired above, plus a third, more fundamental one verified via re-run: Firefox fires additional onUpdated changeInfo updates -- observed isArticle:false -- that Chrome has no equivalent for at all (Firefox's Reader-Mode-eligibility heuristic runs on every navigation; Chrome's tabs API has no Reader Mode concept). This is a permanent, well-documented structural difference in the tabs.onUpdated changeInfo surface between engines, confirmed by direct observation (not inferred), and not something any converter or harness fix can reconcile -- there's no Chrome-side event to normalize toward.",
        "contextMenus.*": "See _offscreen_cascade_root_cause -- contextMenus.create (one per clip action) and contextMenus.removeAll are called from registerContextMenuItems(), itself gated behind the same clipperIdProcessed promise; never runs on Firefox.",
        "management.uninstallSelf*": "Kept from the original task spec as a defensive/regression-guard pattern. Investigated: this pinned version (3.11.2) does not declare the `management` permission at all and never calls management.uninstallSelf in either trace, so the pattern is currently inert (matches nothing). Left in place in case a future re-pin reintroduces browser-detection + self-uninstall behavior, per the binding decision that this entry exists partly to catch that regression."
      },
      "quarantined": false
    }
  ]
}
