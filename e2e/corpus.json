{
  "extensions": [
    {
      "id": "latextocalc",
      "name": "LatexToCalc",
      "source": "local:../LatexToCalc",
      "allowed_diffs": ["tabs.create:resolve"],
      "_notes": {
        "_history": "tabs.create:resolve used to be allowlisted here for the native Chrome-vs-Firefox tabs.Tab shape difference (Chrome-only: selected, frozen; Firefox-only: attention, hidden, isArticle, isInReaderMode, sharingState, successorTabId, cookieStoreId). Fixed properly in diff.ts's normalizeTrace instead: any Tab-shaped object (id+windowId+index+boolean active) is now projected down to {url, title, status, index, active} before comparison, so that entire class of divergence is gone.",
        "tabs.create:resolve": "Much smaller residual after the shape-projection fix: verified via re-run that the ONLY remaining difference is the `status` field (chrome: \"loading\", firefox: \"complete\") -- both sides otherwise now normalize identically (index, active match; url/title absent on both, matching this extension's permission scope). This is a genuine browser-native timing race in when each engine resolves the tabs.create() promise relative to the new tab's initial paint, not extension or converter behavior, and not fixable by adjusting probe delays (the capture happens synchronously in the shim's promise-then hook, with no artificial wait in between)."
      },
      "quarantined": false
    },
    {
      "id": "gojbdfnpnhogfdgjbigejoaolejmgdhk",
      "name": "OneNote Web Clipper",
      "source": "cws",
      "version": "3.11.2",
      "allowed_diffs": [
        "offscreen.*",
        "runtime.getContexts*",
        "runtime.getURL#<ext>/offscreen.html",
        "runtime.sendMessage#offscreen",
        "runtime.error#JSON.parse: unexpected character",
        "runtime.getManifest",
        "net.fetch#onenote.com/strings",
        "tabs.query#lastFocusedWindow",
        "tabs.create#getting-started",
        "tabs.create:resolve",
        "tabs.onCreated:fired",
        "tabs.onUpdated:fired#getting-started",
        "contextMenus.*",
        "management.uninstallSelf*"
      ],
      "_notes": {
        "_offscreen_cascade_root_cause": "HISTORICAL (pre-polyfill), root cause #1. OneNote's background script (minified from build/scripts/communicator/offscreenCommunicator.js) calls chrome.runtime.getContexts({contextTypes:[chrome.runtime.ContextType.OFFSCREEN_DOCUMENT], ...}) unconditionally, with no chrome.offscreen feature-detection guard. Before the runtime offscreen polyfill (issue #2), chrome.runtime.ContextType.OFFSCREEN_DOCUMENT was undefined on Firefox and this call threw synchronously ('Expected string instead of undefined'), which silently killed OneNote's entire post-install setup (registerContextMenuItems(), onInstalled(), and invokeClipperInTab()'s actual clip-trigger path all chained off the same unhandled-rejecting clipperIdProcessed promise) -- Firefox produced only 8 traced background events vs Chrome's 36 (measured in this environment at re-triage time; see issue #2 comment for the full before/after numbers). The converter now ships a runtime chrome.offscreen polyfill (chrome.runtime.ContextType.OFFSCREEN_DOCUMENT + a real createDocument/closeDocument backed by a hidden iframe) that unblocks this specific cascade trigger: Firefox now reaches getContexts and createDocument, and the offscreen document itself loads -- but the first message round-trip TO it then fails (see below). See _offscreen_relay_root_cause below for the SECOND, independent root cause the polyfill exposes one layer deeper -- OneNote's clipperIdProcessed chain still never resolves on Firefox, just from a different trigger, so the SAME downstream patterns (getManifest, tabs.create tour, contextMenus.*, etc.) remain chrome-only for a different reason than before.",
        "_offscreen_relay_root_cause": "NEW, post-polyfill, root cause #2 -- filed as https://github.com/OtsoBear/chrome2moz/issues/8. offscreen.js's chrome.runtime.onMessage listener is declared `async function(e,a,s){...}` and calls sendResponse (`s(...)`) synchronously inside, without ever `return true`-ing. On Chrome, the manual sendResponse call wins and the background's sendMessage callback receives the JSON string offscreen.js sent. On Firefox (via the extension's own direct `chrome.*` calls -- no chrome2moz-authored shim is in this call path), the callback instead receives `undefined` -- consistent with Firefox's native chrome.* compat surface using the async listener's own implicit Promise resolution instead of the manually-invoked sendResponse value. The background's callback then does `JSON.parse(e)` on that `undefined`, which coerces to `JSON.parse(\"undefined\")` and throws `SyntaxError: JSON.parse: unexpected character at line 1 column 1 of the JSON data`. This is, again, a latent bug in OneNote's own source (mixing an async listener with manual sendResponse is a documented Chrome-extension footgun) that chrome2moz can't rewrite -- not a chrome2moz mis-conversion, same reasoning as issue #2 and the original _offscreen_cascade_root_cause. Reproduced identically across two consecutive `pnpm e2e --only gojbdfnpnhogfdgjbigejoaolejmgdhk` runs (deterministic, not flake): Firefox trace ends at 7 background events, one FEWER than the pre-polyfill baseline of 8 -- but that is NOT a sign Firefox crashes earlier; it crashes LATER, further into OneNote's code, than the old getContexts throw did (getContexts succeeds, createDocument succeeds, the offscreen document loads, and the first relay message is sent before this crash). The lower raw count is an artifact of issue #6: getContexts and createDocument now run through the polyfill's untraced surface (see the offscreen.*/runtime.getContexts* notes below), so several genuine steps of forward progress produce zero trace events, more than offsetting the one new event (runtime.getURL) and one new terminal error (runtime.error) this path adds. Raw Firefox event count is therefore NOT a valid progress metric for this entry -- it fell from 8 to 7 while Firefox objectively executed further into the extension's code than before. Pre-polyfill baseline of 8 is from prior run artifacts (the corpus entry's history before this task); the original 8 event names were not preserved and are not re-derivable from this session (the pre-polyfill trace file was overwritten by later runs in this same task, and results/ is gitignored).",
        "offscreen.*": "Issue #6 tracing blind spot (same as the offscreen-gate corpus entry's note): Firefox-side offscreen.* calls hit the converter's polyfill, injected AFTER the spy shim walks the API surface, so offscreen.createDocument/:resolve are untraced on that side and appear chrome-only.",
        "runtime.getContexts*": "Issue #6 tracing blind spot (same as the offscreen-gate corpus entry's note): the polyfill's getContexts wrapper replaces chrome.runtime.getContexts directly (shims/offscreen-polyfill.js) and, for OneNote's exact filter ({contextTypes:[OFFSCREEN_DOCUMENT], documentUrls:[...]}), never calls through to the spy-wrapped original -- so getContexts/:resolve are untraced chrome-only on Firefox. Functionally verified equivalent: both sides reach the same offscreen-relay code path afterward (see _offscreen_relay_root_cause).",
        "runtime.getURL#<ext>/offscreen.html": "Issue #6 tracing blind spot (same as the offscreen-gate corpus entry's note): the polyfill's offscreen.createDocument implementation calls rt.getURL(url) itself to resolve the iframe src, on top of OneNote's own getURL(\"offscreen.html\") call -- this one runs through the untouched, spy-wrapped original, so it shows up as one extra firefox-only call. Pinned to the literal observed args, \"<ext>/offscreen.html\" -- unlike the offscreen-gate entry's plain \"offscreen.html\", OneNote's own code resolves the URL to an absolute extension URL BEFORE passing it into createDocument's {url} option, so when the polyfill's rt.getURL(url) call receives that already-absolute URL, the trace normalizer rewrites it to the <ext>/... form. Verified against the actual trace file, not assumed.",
        "runtime.sendMessage#offscreen": "See _offscreen_relay_root_cause (issue #8) -- once Firefox's clipperId relay crashes on the first get-from-local-storage round-trip, the subsequent remove-from-local-storage/set-to-local-storage (clipperId, locale, locStrings) sendMessage calls that Chrome makes never happen on Firefox at all. Pinned to the substring \"offscreen\" via api#substring (all of these carry target:\"offscreen\" payloads) so other runtime.sendMessage call sites (contentCaptureInject.js, logManager.js, regionOverlay.js -- the actual clip flow, not currently probed) are NOT masked if probes are later extended to exercise them.",
        "runtime.error#JSON.parse: unexpected character": "See _offscreen_relay_root_cause (issue #8) -- the SyntaxError itself, surfaced via the shim's global error/unhandledrejection listeners. Pinned to the substring \"JSON.parse: unexpected character\" via api#substring so an unrelated runtime.error for this extension is NOT masked. Distinct pattern from the old (now-removed) runtime.error#for runtime.getContexts allowance, which covered the pre-polyfill getContexts TypeError and no longer fires.",
        "runtime.getManifest": "See _offscreen_relay_root_cause -- called from within the gated onInstalled() setup (chrome-only in the trace; the gate is now issue #8's crash rather than the old getContexts throw, but the effect on this call site is identical). Left name-only (not pinned): OneNote calls getManifest with no arguments, so there's no argument substring to pin to, and the API has no other call sites in this extension.",
        "net.fetch#onenote.com/strings": "See _offscreen_relay_root_cause -- pinned to the exact observed URL (GET https://www.onenote.com/strings?... -- localized UI strings for the getting-started tour/context menu), fetched only from within the gated onInstalled() path. A net.fetch divergence to any other URL is NOT masked.",
        "tabs.query#lastFocusedWindow": "See _offscreen_relay_root_cause -- used to find the active tab for the getting-started tour ({active:true, lastFocusedWindow:true}), inside the gated onInstalled() path. Pinned to \"lastFocusedWindow\" via api#substring; Call args only (a query filter object, not a Tab result), so the Tab-shape normalizer fix doesn't apply here -- Firefox simply never reaches this call at all.",
        "tabs.create#getting-started": "See _offscreen_relay_root_cause -- opens the getting-started tour tab (support.microsoft.com/.../getting-started-with-the-onenote-web-clipper-...), inside the gated onInstalled() path; never called on Firefox at all (not a shape difference -- the call itself doesn't happen), so the Tab-shape normalizer can't help here. Pinned to \"getting-started\" via api#substring.",
        "tabs.create:resolve": "Verified via re-run this is still ONLY the offscreen-cascade tour tab, chrome-only: Firefox never calls tabs.create for it at all (zero events on that side, not a shape or timing mismatch). Left name-only (not pinned): at resolve time the tab's url/title are still empty ({url:\"\",title:\"\",status:\"loading\",...} -- the promise resolves before navigation starts), so there is no distinctive substring to pin to yet. Distinct from latextocalc's tabs.create:resolve allowance, which is a real cross-browser timing race present on BOTH sides.",
        "tabs.onCreated:fired": "Verified via re-run this is still ONLY the offscreen-cascade tour tab, chrome-only -- the fixture-tab source it used to also cover (OneNote's own tabs.onCreated/onUpdated listener observing the harness's probe-driven page-opens) remains gone (pingProbe gating, see below). Left name-only (not pinned): onCreated's snapshot is {url:\"\",title:\"\",status:\"loading\",index:1,active:true} -- empty url/title, no distinctive substring available (the tab hasn't started navigating yet when onCreated fires).",
        "tabs.onUpdated:fired#getting-started": "Same source as tabs.onCreated:fired above (the tour tab's own navigation, chrome-only). Every remaining event's changeInfo/tab carries the tour tab's URL once navigation starts, so pinned to \"getting-started\" via api#substring.",
        "pingProbe (harness fix, not an allowed_diffs entry)": "probes.ts's pingProbe checks contentScriptsCoverUrl(manifest, fixtureUrl) BEFORE opening any pages (same check contentProbe already used), skipping immediately when content_scripts can't cover the fixture origin -- which is OneNote's case (content_scripts only match onenote.officeapps.live.com). This keeps the harness's own probe-driven fixture-tab opens from producing a second, unrelated source of tabs.onCreated:fired/tabs.onUpdated:fired divergence tangled up with the tour-tab one documented above.",
        "contextMenus.*": "See _offscreen_relay_root_cause -- contextMenus.create (one per clip action) and contextMenus.removeAll are called from registerContextMenuItems(), itself gated behind the same clipperIdProcessed promise; never runs on Firefox.",
        "management.uninstallSelf*": "Kept from the original task spec as a defensive/regression-guard pattern. Investigated: this pinned version (3.11.2) does not declare the `management` permission at all and never calls management.uninstallSelf in either trace, so the pattern is currently inert (matches nothing). Left in place in case a future re-pin reintroduces browser-detection + self-uninstall behavior, per the binding decision that this entry exists partly to catch that regression."
      },
      "_note": "gates nothing until a fixture host matching its content_scripts (onenote.officeapps.live.com) exists -- tracked in issue #5",
      "quarantined": true
    },
    {
      "id": "offscreen-gate",
      "name": "C2M Offscreen Gate",
      "source": "local:testdata/offscreen-extension",
      "allowed_diffs": [
        "offscreen.*",
        "runtime.getContexts*",
        "runtime.getURL#offscreen.html",
        "runtime.sendMessage:resolve#hello async offscreen",
        "runtime.sendMessage:resolve#null",
        "storage.local.set#hello async offscreen",
        "storage.local.set#UNDEFINED_RESPONSE"
      ],
      "quarantined": false,
      "_notes": {
        "offscreen.*": "Firefox-side offscreen.* calls hit the converter's polyfill, which is injected AFTER the spy shim walks the API surface (issue #6) -- so they are untraced on that side and appear chrome-only (offscreen.createDocument / :resolve). Behavioral equivalence is asserted by the storage.local.set offscreenResult write, which must match on both sides.",
        "runtime.getContexts*": "Same issue #6 root cause, one layer deeper than expected: the polyfill's getContexts wrapper replaces chrome.runtime.getContexts directly (shims/offscreen-polyfill.js), so on Firefox the call never reaches the spy-wrapped original at all when the extension's contextTypes filter is exactly [OFFSCREEN_DOCUMENT] -- the wrapper intentionally skips calling through to the real getContexts in that case (an empty contextTypes array matches everything in the native API, so it must never be passed through). Verified via trace inspection this is a tracing gap, not a functional one: Chrome's real getContexts call resolves to [] (no offscreen doc registered yet) and the extension proceeds identically on both sides. Side-agnostic masking caveat: because this pattern has no substring pin, it would also mask a REAL functional regression in the polyfill's getContexts wrapper (e.g. it started returning wrong results instead of just being untraced) -- the entry still catches that class of regression via the storage.local.set offscreenResult value assertion below, which is content-pinned and would diverge if the wrapper's behavior actually broke.",
        "runtime.getURL#offscreen.html": "The polyfill's offscreen.createDocument implementation calls rt.getURL(url) itself (to resolve the iframe src), in addition to the extension's own t.getURL(\"offscreen.html\") call -- so Firefox observes one extra runtime.getURL call the spy shim did capture (this one runs through the untouched original, unlike getContexts above). Pinned to the literal URL since this extension has only one; a runtime.getURL divergence for any other URL is NOT masked.",
        "_issue_8_regression_control": "The extension's second onMessage listener (offscreen.js) and second viaOffscreen(...,\"parse-async\") call (background.js) are a deliberate, decisive control for chrome2moz issue #8: an `async` onMessage listener that calls sendResponse synchronously without `return true`, the exact shape of OneNote Web Clipper's offscreen-relay listener. Confirmed reproducible (2 consecutive runs, identical result): Chrome's sendMessage promise resolves to the value manually passed to sendResponse ({text:\"hello async offscreen\"}); Firefox's instead resolves to undefined (serialized as null in the trace), discarding the manual sendResponse call in favor of the async listener's own implicit (undefined) resolution. This is an INTENTIONAL, permanent divergence -- a regression gate for issue #8's incompatibility class, not a tracing artifact or an accepted by-design difference to quietly work around. Delete the four patterns below (runtime.sendMessage:resolve#null, runtime.sendMessage:resolve#hello async offscreen, storage.local.set#UNDEFINED_RESPONSE, storage.local.set#hello async offscreen) once chrome2moz ships a compat wrapper for this pattern (see issue #8's suggested fix) -- at that point this probe should start passing with NO allowance, and its continued failure would mean the fix regressed.",
        "runtime.sendMessage:resolve#hello async offscreen": "See _issue_8_regression_control -- Chrome side of the pair: the real value offscreen.js's second listener passed to sendResponse.",
        "runtime.sendMessage:resolve#null": "See _issue_8_regression_control -- Firefox side of the pair: the sendMessage promise resolves to undefined (serialized as null) instead of the manually-invoked sendResponse value. Pinned to \"null\" rather than a marker string since this is the raw native resolve value, not something the extension controls; acceptable specifically because this gate extension has exactly one call site that can resolve to null (the parse-async probe) -- would need re-deriving if a second null-resolving message kind is ever added here.",
        "storage.local.set#hello async offscreen": "See _issue_8_regression_control -- Chrome side: the real parsed text gets stored because sendResponse's value was honored.",
        "storage.local.set#UNDEFINED_RESPONSE": "See _issue_8_regression_control -- Firefox side: the extension's own UNDEFINED_RESPONSE marker object (not a native browser value; chosen so the divergence is legible in the trace instead of silently vanishing as a dropped-undefined storage key) gets stored because viaOffscreen(\"parse-async\") resolved to undefined instead of the real parsed text."
      }
    }
  ]
}
